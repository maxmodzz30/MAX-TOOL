apt update && apt upgrade
pkg install git
apt update
apt install unzip
apt update && apt upgrade
termux-setup-storage
apt install toilet
dnf install toilet
pkg install libandroid-wordexp
pkg update && pkg upgrade
pkg install zip
apt update
apt install pv

if [ -f $HOME/MAX-FPS/fps ]; then
    chmod +x $HOME/MAX-FPS/fps
    mv $HOME/MAX-FPS/fps $PREFIX/bin
fi

chmod +x $HOME/MAX-FPS/FPS
./FPS
