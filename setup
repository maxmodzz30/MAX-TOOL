apt update && apt upgrade
pkg install git
apt update
apt install unzip
apt update && apt upgrade
termux-setup-storage
apt install toilet
pkg install libandroid-wordexp
pkg update && pkg upgrade
pkg install zip
apt update
apt install pv


if [ -f $HOME/MAX-FPS/FPS ]; then
chmod +x $HOME/MAX-FPS/FPS
cp $HOME/MAX-FPS/FPS $PREFIX/bin
fi

if [ -f $HOME/MAX-FPS/FPS ]; then
chmod +x setup
chmod +x FPS
./FPS
fi
